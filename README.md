# Text to ClickHouse SQL

Программа для преобразования текстовых запросов на русском языке в SQL запросы для ClickHouse с использованием встроенной функции AI SQL generation в ClickHouse.

> 💡 **Хотите использовать ClickHouse CLI напрямую без Python?**
> Смотрите [**Руководство по использованию ClickHouse CLI**](CLICKHOUSE_CLI_GUIDE.md) - подробная инструкция как использовать встроенную AI функцию ClickHouse напрямую в командной строке.

## Описание

Эта программа использует **встроенную функцию AI SQL generation в ClickHouse** для взаимодействия с базой данных на естественном языке. Вы пишете запрос на русском языке, а AI-агент автоматически формулирует правильный SQL запрос с автоматическим исследованием схемы базы данных.

## Функциональность

- 🤖 Генерация SQL запросов из текста на русском языке через встроенный AI ClickHouse
- 🔍 Автоматическое исследование схемы базы данных (schema discovery)
- 🎯 Поддержка нескольких AI провайдеров: OpenRouter, Anthropic Claude, OpenAI GPT
- 🔌 Подключение к ClickHouse через HTTP/HTTPS интерфейс (порты 8443, 8123) или через `clickhouse-client` (порт 9440)
- ▶️ Выполнение сгенерированных SQL запросов
- 📋 Форматированный вывод результатов
- 🔒 Безопасное хранение учетных данных через переменные окружения

## Требования

- Python 3.7 или выше
- **ClickHouse Client** (clickhouse-client CLI tool)
- Доступ к одному из AI сервисов:
  - OpenRouter (рекомендуется) - поддержка Claude, GPT и других моделей
  - Anthropic Claude API
  - OpenAI GPT API
- Доступ к базе данных ClickHouse

## Установка

### 1. Установка ClickHouse Client

**Linux (Debian/Ubuntu):**
```bash
sudo apt-get install -y apt-transport-https ca-certificates dirmngr
sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv 8919F6BD2B48D754
echo "deb https://packages.clickhouse.com/deb stable main" | sudo tee /etc/apt/sources.list.d/clickhouse.list
sudo apt-get update
sudo apt-get install -y clickhouse-client
```

**macOS:**
```bash
brew install clickhouse
```

**Проверка установки:**
```bash
clickhouse-client --version
```

### 2. Клонирование репозитория

```bash
git clone https://github.com/Erofaxxx/text_to_clickhouse_sql.git
cd text_to_clickhouse_sql
```

### 3. Создание виртуального окружения (рекомендуется)

```bash
python3 -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows
```

### 4. Установка зависимостей Python

```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте файл `.env` и заполните необходимые значения:

```env
# AI API Configuration (требуется один из следующих ключей)
# Приоритет: OPENROUTER_API_KEY > ANTHROPIC_API_KEY > OPENAI_API_KEY

# Для использования OpenRouter (рекомендуется)
OPENROUTER_API_KEY=ваш_api_ключ_openrouter

# Для использования Anthropic Claude напрямую
# ANTHROPIC_API_KEY=ваш_api_ключ_anthropic

# Для использования OpenAI GPT напрямую
# OPENAI_API_KEY=ваш_api_ключ_openai

# ClickHouse Database Configuration
CLICKHOUSE_HOST=ваш_хост_clickhouse
CLICKHOUSE_PORT=8443
CLICKHOUSE_USER=ваше_имя_пользователя
CLICKHOUSE_PASSWORD=ваш_пароль
CLICKHOUSE_DATABASE=имя_базы_данных
CLICKHOUSE_TABLE=имя_таблицы
CLICKHOUSE_SSL_CERT_PATH=путь_к_сертификату_ssl  # необязательно
```

**Важно:** Файл `.env` не должен попадать в систему контроля версий (уже добавлен в `.gitignore`).

### 6. Получение API ключа

#### Вариант A: OpenRouter (рекомендуется)

1. Зарегистрируйтесь на [OpenRouter](https://openrouter.ai/)
2. Перейдите в раздел API Keys
3. Создайте новый API ключ
4. Скопируйте ключ и вставьте в `.env` как `OPENROUTER_API_KEY`

Преимущества OpenRouter:
- Доступ к множеству моделей (Claude, GPT, и др.)
- Единый интерфейс для разных провайдеров
- Гибкое ценообразование

#### Вариант B: Anthropic Claude

1. Зарегистрируйтесь на [Anthropic Console](https://console.anthropic.com/)
2. Создайте API ключ
3. Вставьте в `.env` как `ANTHROPIC_API_KEY`

#### Вариант C: OpenAI

1. Зарегистрируйтесь на [OpenAI Platform](https://platform.openai.com/)
2. Создайте API ключ
3. Вставьте в `.env` как `OPENAI_API_KEY`

### 7. Настройка SSL сертификата (при необходимости)

Если ваш ClickHouse сервер использует самоподписанный сертификат:

1. Скачайте файл сертификата (например, `YandexInternalRootCA.crt`)
2. Поместите его в директорию проекта
3. Укажите путь к сертификату в `.env`:

```env
CLICKHOUSE_SSL_CERT_PATH=YandexInternalRootCA.crt
```

## Как это работает

Программа использует **встроенную функцию AI SQL generation в ClickHouse**, которая:

1. **Автоматически исследует схему БД** - AI изучает доступные базы данных, таблицы и их структуру
2. **Генерирует точный SQL** - создает запросы с правильными именами таблиц, колонок и JOIN'ами
3. **Использует правильный синтаксис ClickHouse** - учитывает особенности ClickHouse SQL

Под капотом:
- Программа создает временный конфигурационный файл для ClickHouse Client
- Запросы передаются в `clickhouse-client` с префиксом `??`
- ClickHouse AI исследует схему и генерирует SQL
- Результат возвращается в Python программу для отображения

## Запуск программы

```bash
python3 text_to_sql.py
```

## Использование

После запуска программы вы увидите приветственное сообщение и командную строку:

```
💬 Ваш запрос:
```

### Примеры запросов

- "Покажи последние 10 визитов"
- "Сколько всего записей в таблице"
- "Покажи уникальных посетителей за последнюю неделю"
- "Выведи топ 5 самых популярных страниц"

### Доступные команды

- `help` или `помощь` - показать справку
- `schema` или `схема` - показать схему таблицы
- `exit` или `выход` - выйти из программы

### Рабочий процесс

1. Введите запрос на русском языке
2. Программа сгенерирует SQL запрос
3. Программа покажет сгенерированный SQL
4. Программа спросит, хотите ли вы выполнить запрос
5. При подтверждении запрос будет выполнен, и результаты будут показаны

## Примеры работы

### Пример 1: Простой запрос с автоматическим исследованием схемы

```
💬 Ваш запрос: Покажи последние 5 записей

⏳ Генерация SQL запроса с использованием ClickHouse AI...

📝 Сгенерированный SQL:
SELECT * FROM ym_sanok.visits_complete ORDER BY visit_date DESC LIMIT 5

▶ Выполнить запрос? (y/n): y

✓ Результаты запроса:
id | visit_date | page_url | ...
---+------------+----------+-----
1  | 2024-01-15 | /home    | ...
...
```

### Пример 2: Агрегация данных

```
💬 Ваш запрос: Сколько всего визитов за сегодня

⏳ Генерация SQL запроса с использованием ClickHouse AI...

📝 Сгенерированный SQL:
SELECT COUNT(*) as total_visits FROM ym_sanok.visits_complete WHERE toDate(visit_date) = today()

▶ Выполнить запрос? (y/n): y

✓ Результаты запроса:
total_visits
------------
142

Всего строк: 1
```

## Структура проекта

```
text_to_clickhouse_sql/
├── text_to_sql.py      # Основной файл программы
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример файла с переменными окружения
├── .env                # Ваши переменные окружения (не в git)
├── .gitignore          # Файлы, игнорируемые git
├── README.md           # Документация (этот файл)
├── QUICKSTART.md       # Краткое руководство по запуску
├── prompt.txt          # Исходное описание задачи
└── add-prompt.txt      # Информация о встроенной AI функции ClickHouse
```

## Зависимости

- **ClickHouse Client** - CLI инструмент для работы с ClickHouse (со встроенной AI функцией)
- `python-dotenv` - загрузка переменных окружения из файла .env

## Преимущества встроенной AI функции ClickHouse

✅ **Автоматическое исследование схемы** - AI сам изучает структуру вашей базы данных  
✅ **Точные запросы** - генерирует SQL с правильными именами таблиц и колонок  
✅ **Поддержка сложных запросов** - JOIN'ы, подзапросы, агрегации  
✅ **Нативная интеграция** - использует официальный механизм ClickHouse  
✅ **Гибкость** - поддержка разных AI провайдеров (OpenRouter, Anthropic, OpenAI)

## Безопасность

⚠️ **Важные замечания по безопасности:**

- Никогда не коммитьте файл `.env` в систему контроля версий
- Храните API ключи и пароли в безопасности
- Используйте переменные окружения для конфиденциальных данных
- Регулярно обновляйте API ключи
- Используйте SSL/TLS для подключения к базе данных
- AI видит только схему БД (структуру таблиц), не сами данные

## Устранение неполадок

### Ошибка: "clickhouse-client: command not found"

ClickHouse Client не установлен. Установите его:
```bash
# Linux (Debian/Ubuntu)
sudo apt-get install clickhouse-client

# macOS
brew install clickhouse
```

### Ошибка подключения к ClickHouse

Если вы видите ошибку подключения:
- Проверьте правильность хоста, порта и учетных данных в `.env`
- Убедитесь, что ваш IP разрешен в настройках ClickHouse
- Проверьте доступность сервера
- Проверьте SSL сертификат (если используется)

**Ошибка `Unexpected packet from server (expected Hello or Exception, got )`:**

Эта ошибка возникает, когда `clickhouse-client` пытается подключиться к HTTP-порту (8443) вместо нативного TCP-порта (9440). Программа автоматически определяет тип порта и использует правильный протокол:
- **Порты 8443, 443, 8123** → подключение через HTTP/HTTPS интерфейс
- **Порты 9440, 9000** → подключение через `clickhouse-client` (нативный протокол)

Если нативный порт на вашем сервере отличается от 9440, укажите его в `.env`:
```env
CLICKHOUSE_NATIVE_PORT=9000  # например, для нативного порта без TLS
```

### Ошибка AI API

Если AI генерация не работает:
- Проверьте правильность API ключа (OPENROUTER_API_KEY, ANTHROPIC_API_KEY, или OPENAI_API_KEY)
- Убедитесь, что у вас есть кредиты на выбранном сервисе
- Проверьте интернет-соединение

### AI ищет в неправильной базе данных (при использовании ClickHouse CLI напрямую)

Если вы используете `clickhouse-client` напрямую и AI ищет таблицы в базе данных 'default' вместо вашей базы:

**Быстрое решение:**
```sql
-- Укажите базу данных явно в запросе
:) ?? покажи таблицы в базе данных ym_sanok

-- Или переключитесь на нужную базу
:) USE ym_sanok;
:) ?? покажи последние 10 записей
```

**Подробное объяснение и все решения:** См. [CLICKHOUSE_CLI_GUIDE.md - раздел "AI ищет таблицы в базе данных 'default'"](CLICKHOUSE_CLI_GUIDE.md#7-ai-ищет-таблицы-в-базе-данных-default-вместо-моей-базы-данных)

### Ошибка импорта модулей

Если вы видите ошибки импорта:
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте, что вы используете правильное виртуальное окружение

## Технические детали

### Как работает интеграция с ClickHouse AI

1. Программа создает временный YAML конфигурационный файл с настройками AI
2. Конфигурация включает API ключ, провайдера, модель и параметры
3. Запросы передаются в `clickhouse-client` CLI с префиксом `??`
4. ClickHouse AI автоматически исследует схему базы данных
5. Генерируется SQL запрос с учетом реальной структуры таблиц
6. SQL возвращается в Python программу для отображения и выполнения

### Поддерживаемые AI провайдеры

| Провайдер | Модель по умолчанию | Переменная окружения |
|-----------|---------------------|---------------------|
| OpenRouter | Claude 3.5 Sonnet | `OPENROUTER_API_KEY` |
| Anthropic | Claude 3.5 Sonnet | `ANTHROPIC_API_KEY` |
| OpenAI | GPT-4o | `OPENAI_API_KEY` |

## Альтернативный способ: Использование ClickHouse CLI напрямую

Если вы хотите использовать AI функцию ClickHouse без Python программы, смотрите подробное руководство:

👉 [**Как использовать ClickHouse CLI напрямую (без Python)**](CLICKHOUSE_CLI_GUIDE.md)

В этом руководстве вы найдете:
- Настройку `clickhouse-client` с AI
- Как использовать `??` префикс для запросов на естественном языке
- Примеры запросов и команд
- Полное устранение неполадок

## Лицензия

MIT License

## Автор

Erofaxxx

## Ссылки

- [ClickHouse Documentation](https://clickhouse.com/docs)
- [ClickHouse AI SQL Generation](https://clickhouse.com/docs/en/interfaces/cli#ai-sql-generation)
- [OpenRouter](https://openrouter.ai/)
- [Anthropic Claude](https://www.anthropic.com/)
- [OpenAI](https://openai.com/)
