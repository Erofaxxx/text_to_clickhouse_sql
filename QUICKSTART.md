# Быстрый старт

## Краткая инструкция по запуску

### 1. Установите ClickHouse Client

**Linux (Debian/Ubuntu):**
```bash
sudo apt-get install clickhouse-client
```

**macOS:**
```bash
brew install clickhouse
```

**Проверка:**
```bash
clickhouse-client --version
```

### 2. Установите Python 3.7+

Убедитесь, что у вас установлен Python версии 3.7 или выше:

```bash
python3 --version
```

### 3. Клонируйте репозиторий

```bash
git clone https://github.com/Erofaxxx/text_to_clickhouse_sql.git
cd text_to_clickhouse_sql
```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
```

Или с виртуальным окружением:

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

pip install -r requirements.txt
```

### 5. Настройте .env файл

```bash
cp .env.example .env
nano .env  # или используйте любой текстовый редактор
```

Заполните обязательные поля:

**AI API ключ (требуется один из):**
- `OPENROUTER_API_KEY` - ключ от OpenRouter (рекомендуется) - https://openrouter.ai/
- `ANTHROPIC_API_KEY` - ключ от Anthropic - https://console.anthropic.com/
- `OPENAI_API_KEY` - ключ от OpenAI - https://platform.openai.com/

**Настройки ClickHouse:**
- `CLICKHOUSE_HOST` - хост вашей БД ClickHouse
- `CLICKHOUSE_USER` - имя пользователя
- `CLICKHOUSE_PASSWORD` - пароль
- `CLICKHOUSE_DATABASE` - имя базы данных
- `CLICKHOUSE_TABLE` - имя таблицы

### 6. Запустите программу

```bash
python3 text_to_sql.py
```

### 7. Используйте программу

Просто пишите запросы на русском языке:

```
💬 Ваш запрос: Покажи последние 10 записей
```

Программа использует **встроенную функцию AI в ClickHouse**, которая:
- ✅ Автоматически исследует схему вашей базы данных
- ✅ Генерирует точный SQL запрос
- ✅ Учитывает специфику ClickHouse

## Команды в программе

- `help` - справка
- `schema` - показать схему таблицы
- `exit` - выход

## Примеры запросов

1. "Покажи последние 10 визитов"
2. "Сколько всего записей в таблице"
3. "Покажи уникальных посетителей"
4. "Выведи топ 5 популярных страниц"
5. "Посчитай количество визитов по дням за последний месяц"

## Что изменилось?

**Новый подход:** Программа теперь использует встроенную AI функцию ClickHouse вместо прямых вызовов к OpenRouter API.

**Преимущества:**
- 🔍 Автоматическое исследование схемы БД
- 🎯 Более точные SQL запросы
- 🚀 Нативная интеграция с ClickHouse
- 🔄 Поддержка разных AI провайдеров

## Если что-то не работает

1. **"clickhouse-client: command not found"**
   - Установите ClickHouse Client (см. шаг 1)

2. **Ошибка AI API**
   - Проверьте, что API ключ правильно указан в `.env`
   - Убедитесь, что у вас есть кредиты на выбранном сервисе

3. **Ошибка подключения к ClickHouse**
   - Проверьте настройки подключения в `.env`
   - Убедитесь, что сервер доступен

4. **Ошибка импорта модулей**
   - Выполните: `pip install -r requirements.txt`
   - Проверьте активацию виртуального окружения

Подробная документация: см. [README.md](README.md)
