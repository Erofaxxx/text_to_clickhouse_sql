# Как использовать ClickHouse CLI напрямую (без Python)

## Введение

Этот гайд показывает, как использовать **встроенную AI функцию ClickHouse** напрямую через `clickhouse-client`, без Python программы. Вы сможете задавать запросы на естественном языке прямо в командной строке ClickHouse.

## Зачем это нужно?

- ✅ Прямая работа с ClickHouse без дополнительных программ
- ✅ Быстрый доступ к AI-генерации SQL
- ✅ Полный контроль над запросами
- ✅ Не нужен Python

## Установка ClickHouse Client

### Linux (Debian/Ubuntu)

```bash
sudo apt-get install -y apt-transport-https ca-certificates dirmngr
sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv 8919F6BD2B48D754
echo "deb https://packages.clickhouse.com/deb stable main" | sudo tee /etc/apt/sources.list.d/clickhouse.list
sudo apt-get update
sudo apt-get install -y clickhouse-client
```

### macOS

```bash
brew install clickhouse
```

### Проверка установки

```bash
clickhouse-client --version
```

Должно показать что-то типа: `ClickHouse client version 24.x.x.x`

## Настройка AI для ClickHouse CLI

У вас есть два варианта настройки:

### Вариант 1: Быстрая настройка через переменные окружения (проще)

Просто установите переменную окружения с вашим API ключом перед запуском `clickhouse-client`:

#### Для OpenAI:
```bash
export OPENAI_API_KEY="ваш-ключ-openai"
```

#### Для Anthropic Claude:
```bash
export ANTHROPIC_API_KEY="ваш-ключ-anthropic"
```

#### Для OpenRouter (рекомендуется):
OpenRouter работает через OpenAI-совместимый API, поэтому нужно создать конфигурационный файл (см. Вариант 2).

### Вариант 2: Конфигурационный файл (больше возможностей)

Создайте конфигурационный файл для ClickHouse Client. Файл должен быть в одном из этих мест:

- `~/.config/clickhouse/config.yaml` (рекомендуется)
- `~/.clickhouse-client/config.yaml` (старое расположение)

#### Пример для OpenRouter (рекомендуется):

```bash
# Создайте директорию если её нет
mkdir -p ~/.config/clickhouse

# Создайте конфигурационный файл
cat > ~/.config/clickhouse/config.yaml << 'EOF'
ai:
  provider: openai
  api_key: ваш-ключ-openrouter
  base_url: https://openrouter.ai/api/v1
  model: anthropic/claude-3.5-sonnet
  temperature: 0.0
  max_tokens: 1000
  timeout_seconds: 30
  enable_schema_access: true
EOF
```

**Где взять ключ OpenRouter:**
1. Зарегистрируйтесь на https://openrouter.ai/
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Вставьте его в config.yaml вместо `ваш-ключ-openrouter`

#### Пример для OpenAI:

```yaml
ai:
  provider: openai
  api_key: ваш-ключ-openai
  model: gpt-4o
  temperature: 0.0
  max_tokens: 1000
  enable_schema_access: true
```

**Где взять ключ OpenAI:**
- https://platform.openai.com/api-keys

#### Пример для Anthropic Claude:

```yaml
ai:
  provider: anthropic
  api_key: ваш-ключ-anthropic
  model: claude-3-5-sonnet-20241022
  temperature: 0.0
  max_tokens: 1000
  enable_schema_access: true
```

**Где взять ключ Anthropic:**
- https://console.anthropic.com/

### Параметры конфигурации (опционально)

Вы можете настроить поведение AI:

```yaml
ai:
  provider: openai
  api_key: ваш-ключ
  model: gpt-4o                    # Модель для использования
  temperature: 0.0                  # 0.0 = детерминированный, 1.0 = творческий
  max_tokens: 1000                  # Максимальная длина ответа
  timeout_seconds: 30               # Таймаут запроса
  enable_schema_access: true        # Разрешить AI исследовать схему БД
  max_steps: 10                     # Максимум шагов для исследования схемы
```

## Подключение к ClickHouse

### Базовое подключение

```bash
clickhouse-client \
  --host ваш-хост.net \
  --port 9440 \
  --user ваш-пользователь \
  --password ваш-пароль \
  --database ваша-база \
  --secure
```

**Важно:** Используйте **нативный порт** ClickHouse:
- `9440` - для защищенного соединения (с TLS/SSL)
- `9000` - для незащищенного соединения

**НЕ используйте HTTP порты** (8443, 8123) - они не работают с `clickhouse-client`!

### Сохранение настроек подключения

Чтобы не вводить параметры подключения каждый раз, добавьте их в конфигурационный файл:

```yaml
# ~/.config/clickhouse/config.yaml
host: ваш-хост.net
port: 9440
user: ваш-пользователь
password: ваш-пароль
database: ваша-база
secure: true

# AI настройки (см. выше)
ai:
  provider: openai
  api_key: ваш-ключ
  # ...
```

Теперь можно просто запускать:

```bash
clickhouse-client
```

### Для Yandex Cloud ClickHouse

Если используете Yandex Managed Service for ClickHouse:

```bash
clickhouse-client \
  --host ваш-кластер.mdb.yandexcloud.net \
  --port 9440 \
  --user ваш-пользователь \
  --password ваш-пароль \
  --database ваша-база \
  --secure \
  --config-file ~/.config/clickhouse/config.yaml
```

Если нужен SSL сертификат:

```bash
# Скачайте сертификат Yandex Cloud
wget "https://storage.yandexcloud.net/cloud-certs/CA.pem" -O ~/.clickhouse-client/root.crt

# Используйте при подключении
clickhouse-client \
  --host ваш-кластер.mdb.yandexcloud.net \
  --port 9440 \
  --user ваш-пользователь \
  --password ваш-пароль \
  --database ваша-база \
  --secure \
  --config-file ~/.config/clickhouse/config.yaml
```

## Использование AI для генерации SQL

### Основной синтаксис

После подключения к ClickHouse, используйте префикс `??` перед вашим запросом на естественном языке:

```sql
:) ?? покажи последние 10 записей из таблицы
```

### Что происходит при выполнении запроса с ??:

1. **AI исследует схему** - автоматически узнает какие таблицы и колонки есть в вашей базе
2. **Генерирует SQL** - создает правильный SQL запрос
3. **Выполняет запрос** - сразу показывает результаты

### Примеры запросов

#### Простой вывод данных:

```sql
:) ?? покажи последние 10 визитов
```

AI сгенерирует что-то типа:
```sql
SELECT * FROM visits_complete
ORDER BY visit_date DESC
LIMIT 10
```

#### Агрегация:

```sql
:) ?? сколько всего записей в таблице
```

```sql
:) ?? посчитай количество уникальных посетителей
```

#### Группировка:

```sql
:) ?? покажи топ 5 самых популярных страниц
```

```sql
:) ?? посчитай визиты по дням за последнюю неделю
```

#### Информация о схеме:

```sql
:) ?? покажи названия столбцов и их типы данных
```

```sql
:) ?? какие таблицы есть в базе данных
```

#### Фильтрация:

```sql
:) ?? покажи визиты за вчерашний день
```

```sql
:) ?? найди визиты где продолжительность больше 5 минут
```

### Как выглядит процесс генерации

Когда вы запускаете запрос с `??`, ClickHouse показывает процесс работы AI:

```
:) ?? покажи последние 5 записей

Starting AI SQL generation with schema discovery...
──────────────────────────────────────────────────

🔍 list_databases
   ➜ system, default, mydb

🔍 list_tables_in_database
   database: mydb
   ➜ visits_complete, users, products

🔍 get_schema_for_table
   database: mydb
   table: visits_complete
   ➜ CREATE TABLE visits_complete (
        id UInt64,
        visit_date DateTime,
        user_id String,
        page_url String,
        duration UInt32
     ) ENGINE = MergeTree()
     ORDER BY visit_date

✨ SQL query generated successfully!
──────────────────────────────────────────────────

SELECT *
FROM mydb.visits_complete
ORDER BY visit_date DESC
LIMIT 5

┌─id─┬─visit_date──────────┬─user_id─┬─page_url────┬─duration─┐
│ 1  │ 2024-01-15 14:30:00 │ user123 │ /home       │ 120      │
│ 2  │ 2024-01-15 14:25:00 │ user456 │ /products   │ 300      │
│ 3  │ 2024-01-15 14:20:00 │ user789 │ /about      │ 45       │
│ 4  │ 2024-01-15 14:15:00 │ user123 │ /contact    │ 90       │
│ 5  │ 2024-01-15 14:10:00 │ user321 │ /home       │ 200      │
└────┴─────────────────────┴─────────┴─────────────┴──────────┘

5 rows in set. Elapsed: 2.341 sec.
```

### Обычные SQL запросы

Конечно, вы также можете выполнять обычные SQL запросы без AI:

```sql
:) SELECT * FROM visits_complete LIMIT 5;
```

```sql
:) DESCRIBE TABLE visits_complete;
```

```sql
:) SHOW TABLES;
```

## Полезные команды ClickHouse CLI

### Команды интерфейса:

```sql
\q или exit    -- Выйти из clickhouse-client
\h или help    -- Показать справку
\d             -- Показать все таблицы
\d table_name  -- Показать структуру таблицы
\c             -- Очистить экран
```

### Системные запросы:

```sql
-- Показать все базы данных
SHOW DATABASES;

-- Показать таблицы в текущей базе
SHOW TABLES;

-- Показать структуру таблицы
DESCRIBE TABLE your_table;

-- Информация о колонках
SELECT name, type, default_kind
FROM system.columns
WHERE table = 'your_table' AND database = 'your_database';

-- Статистика использования
SELECT * FROM system.query_log
WHERE type = 'QueryFinish'
ORDER BY event_time DESC
LIMIT 10;
```

### Форматирование вывода:

```sql
-- Вертикальный формат (удобно для широких таблиц)
SELECT * FROM visits_complete LIMIT 1 FORMAT Vertical;

-- JSON формат
SELECT * FROM visits_complete LIMIT 5 FORMAT JSON;

-- CSV формат
SELECT * FROM visits_complete LIMIT 5 FORMAT CSV;

-- Pretty (красивая таблица, по умолчанию)
SELECT * FROM visits_complete LIMIT 5 FORMAT PrettyCompact;
```

## Устранение неполадок

### 1. "AI features are not configured"

**Проблема:** AI не настроен.

**Решение:**
- Проверьте, что установлена переменная окружения (`OPENAI_API_KEY` или `ANTHROPIC_API_KEY`)
- Или создайте конфигурационный файл `~/.config/clickhouse/config.yaml`
- Проверьте правильность API ключа

```bash
# Проверить переменную окружения
echo $OPENAI_API_KEY

# Проверить конфигурационный файл
cat ~/.config/clickhouse/config.yaml
```

### 2. "Connection refused" или "Unexpected packet from server"

**Проблема:** Неправильный порт.

**Решение:** Используйте **нативный порт**:
- Порт `9440` для защищенного соединения (рекомендуется)
- Порт `9000` для незащищенного соединения

**НЕ используйте** HTTP порты (8443, 8123, 443) - они не работают с `clickhouse-client`!

```bash
# Правильно:
clickhouse-client --host myhost.net --port 9440 --secure

# Неправильно:
clickhouse-client --host myhost.net --port 8443 --secure  # ❌ Не работает!
```

### 3. "Authentication failed"

**Проблема:** Неправильные учетные данные.

**Решение:**
- Проверьте имя пользователя и пароль
- Проверьте, что пользователь имеет права на базу данных

### 4. "SSL certificate problem"

**Проблема:** Проблемы с SSL сертификатом.

**Решение для Yandex Cloud:**
```bash
# Скачайте корневой сертификат
wget "https://storage.yandexcloud.net/cloud-certs/CA.pem" \
  -O ~/.clickhouse-client/root.crt

# Используйте в подключении
clickhouse-client \
  --host your-host.mdb.yandexcloud.net \
  --port 9440 \
  --secure
```

### 5. AI генерирует неправильные запросы

**Причины:**
- AI не понял ваш запрос
- Сложная структура базы данных
- Недостаточно контекста

**Решения:**
- Формулируйте запросы более конкретно
- Упоминайте названия таблиц или колонок
- Разбейте сложный запрос на несколько простых
- Используйте обычный SQL для сложных случаев

**Примеры:**

❌ Неясно: `?? покажи данные`

✅ Лучше: `?? покажи последние 10 записей из таблицы visits`

✅ Ещё лучше: `?? покажи visit_date, user_id и page_url из последних 10 записей таблицы visits`

### 6. Медленная генерация SQL

**Причины:**
- Большая база данных с множеством таблиц
- Медленный интернет
- Ограничения API провайдера

**Решения:**
- Используйте более быстрые модели (например, gpt-3.5-turbo вместо gpt-4)
- Уменьшите `max_steps` в конфигурации
- Используйте обычный SQL для частых запросов

### 7. AI ищет таблицы в базе данных 'default' вместо моей базы данных

**Проблема:** При выполнении запроса с `??` AI исследует базу данных 'default' вместо вашей сконфигурированной базы данных (например, 'ym_sanok'):

```
:) ?? сколько таблиц в базе данных

• Starting AI SQL generation with schema discovery...
─────────────────────────────────────────────────
🧠 thinking...
🔧 Calling: list_databases [toulu_01...]
✓ list_databases completed
  └─ Found 6 databases: - INFORMATION_SCHEMA - _system - default - information_sch...
🧠 thinking
🔧 Calling: list_tables_in_database [toulu_01...]
  └─ Args: {"database":"default"}      ← ❌ ИЩЕТ В default ВМЕСТО ym_sanok!
✓ list_tables_in_database completed
  └─ Found 0 tables in database 'default': (No tables found in this database)
─────────────────────────────────────────────────
• ⚠️  No SQL query was generated
```

#### Почему это происходит

ClickHouse AI автоматически **исследует все базы данных** на сервере, чтобы понять, где находятся нужные данные. Даже если вы указали `database: ym_sanok` в конфигурационном файле, AI может выбрать другую базу данных, если:

1. **Ваш запрос недостаточно конкретен** - AI не понимает, в какой базе искать
2. **В вашей базе данных пусто или нет подходящих таблиц**
3. **AI неправильно интерпретировал запрос** и ищет системную информацию
4. **База данных имеет сложную структуру** с множеством схем

Параметр `database:` в config.yaml устанавливает **контекст по умолчанию**, но AI может исследовать и другие базы данных при необходимости.

#### Решения

**Решение 1: Указывайте базу данных явно в запросе** ⭐ Самое эффективное

Упоминайте название вашей базы данных или таблицы в запросе:

```sql
-- ❌ Неясно - AI может искать везде:
:) ?? сколько таблиц в базе данных

-- ✅ Конкретно - AI поймет, что искать в ym_sanok:
:) ?? сколько таблиц в базе данных ym_sanok

-- ✅ Ещё лучше - указать таблицу:
:) ?? покажи последние 10 записей из таблицы visits в базе ym_sanok

-- ✅ Можно просто упомянуть таблицу (если её имя уникально):
:) ?? покажи последние 10 записей из таблицы visits_complete
```

**Решение 2: Используйте обычный SQL для исследования схемы**

Вместо того, чтобы спрашивать AI о структуре, используйте прямые SQL команды:

```sql
-- Показать все базы данных
:) SHOW DATABASES;

-- Показать таблицы в вашей базе
:) SHOW TABLES FROM ym_sanok;

-- Или переключитесь на вашу базу
:) USE ym_sanok;

-- Теперь можно посмотреть таблицы
:) SHOW TABLES;

-- Описание таблицы
:) DESCRIBE TABLE visits_complete;
```

После того как вы узнали, какие таблицы есть, можете использовать AI для генерации запросов:

```sql
:) ?? покажи последние 10 записей из visits_complete
```

**Решение 3: Проверьте правильность настроек базы данных**

Убедитесь, что в вашем config файле база данных указана правильно:

```bash
cat ~/.config/clickhouse/config.yaml
```

Должно быть:

```yaml
host: rc1b-vsrkuug8qh3pkkeg.mdb.yandexcloud.net
port: 9440
user: User_main
password: click_security_house_7659
database: ym_sanok          ← Проверьте эту строку
secure: true

openSSL:
  client:
    caConfig: /root/.clickhouse-client/root.crt

ai:
  provider: anthropic
  api_key: sk-ant-api03-...
  model: claude-sonnet-4-6
  temperature: 0.0
  max_tokens: 1000
  timeout_seconds: 30
  enable_schema_access: true
```

**Решение 4: Переключитесь на нужную базу через USE**

После подключения к ClickHouse, переключитесь на вашу базу данных:

```sql
:) USE ym_sanok;

-- Теперь все запросы будут выполняться в контексте ym_sanok
:) ?? сколько таблиц здесь

:) ?? покажи последние 10 записей
```

**Решение 5: Укажите базу.таблицу явно в запросе**

Даже без упоминания в естественном языке, вы можете попросить AI использовать fully qualified name:

```sql
:) ?? покажи структуру таблицы ym_sanok.visits_complete

:) ?? посчитай записи в ym_sanok.visits_complete

:) ?? покажи последние записи из ym_sanok.visits_complete
```

**Решение 6: Используйте более конкретные формулировки**

AI лучше понимает конкретные запросы с контекстом:

```sql
-- ❌ Слишком общий запрос - AI не знает где искать:
:) ?? сколько таблиц в базе данных

-- ✅ Конкретный запрос с названием базы:
:) ?? сколько таблиц в базе данных ym_sanok

-- ✅ Конкретный запрос с названием таблицы:
:) ?? сколько записей в таблице visits_complete

-- ✅ Запрос с конкретными данными:
:) ?? покажи последние визиты из таблицы visits_complete в базе ym_sanok
```

#### Проверка что база данных не пустая

Иногда AI ищет в 'default', потому что в вашей базе данных действительно нет таблиц. Проверьте:

```sql
-- Посмотрите, есть ли таблицы
:) SHOW TABLES FROM ym_sanok;

-- Посмотрите, есть ли данные
:) SELECT count() FROM ym_sanok.visits_complete;
```

Если таблиц нет - это объясняет, почему AI не находит данные!

#### Пример правильной работы с AI

```sql
-- 1. Подключитесь к ClickHouse
clickhouse-client

-- 2. Переключитесь на вашу базу данных
:) USE ym_sanok;

-- 3. Посмотрите какие таблицы есть (обычный SQL)
:) SHOW TABLES;

┌─name─────────────┐
│ visits_complete  │
│ users            │
│ sessions         │
└──────────────────┘

-- 4. Теперь используйте AI с конкретными названиями таблиц
:) ?? покажи последние 10 записей из visits_complete

• Starting AI SQL generation with schema discovery...
─────────────────────────────────────────────────
🔧 Calling: list_tables_in_database [toulu_01...]
  └─ Args: {"database":"ym_sanok"}     ← ✅ Правильная база данных!
✓ list_tables_in_database completed
  └─ Found 3 tables in database 'ym_sanok': visits_complete, users, sessions

🔧 Calling: get_schema_for_table [toulu_01...]
  └─ Args: {"database":"ym_sanok", "table":"visits_complete"}
✓ get_schema_for_table completed

─────────────────────────────────────────────────

SELECT *
FROM ym_sanok.visits_complete
ORDER BY visit_date DESC
LIMIT 10
```

#### Важные замечания

⚠️ **AI автоматически исследует схему** - это нормальное поведение. Он может смотреть в разные базы данных, чтобы понять структуру.

⚠️ **Параметр `database:` в config.yaml** - это контекст по умолчанию, но не жесткое ограничение. AI может исследовать другие базы данных.

⚠️ **Будьте конкретны** - чем точнее вы формулируете запрос, тем лучше AI понимает, где искать данные.

✅ **Лучшая практика**: Всегда указывайте название базы данных или таблицы в запросе к AI.

### 8. `??` запрос вернул неожиданные данные (HTML/JSON), но AI не сработал

**Проблема:** Вы выполнили запрос с `??`, получили много данных (HTML, JSON), но:
- ❌ API ключ не использовался (токены не потрачены)
- ❌ Не видели процесс AI генерации ("Starting AI SQL generation...")
- ❌ Получили данные, но это не то, что ожидали

**Что произошло:** AI **вообще не запустился**. Вместо этого был выполнен какой-то другой запрос.

#### Возможные причины:

**Причина 1: AI не настроен или настроен неправильно**

ClickHouse проигнорировал `??` потому что AI функция не включена.

Проверка:
```bash
# Посмотрите конфигурацию
cat ~/.config/clickhouse/config.yaml

# Должна быть секция ai: с правильными параметрами
```

Если секции `ai:` нет или API ключ неправильный - AI не будет работать.

**Причина 2: Выполнился запрос из истории команд**

Вы могли случайно нажать стрелку вверх ↑ и выполнить старый SQL запрос.

Проверка:
```bash
# Посмотрите историю команд
tail -20 ~/.clickhouse-client-history
```

**Причина 3: Синтаксическая ошибка в запросе**

Если после `??` есть синтаксическая ошибка, ClickHouse может попытаться выполнить что-то другое.

**Причина 4: Версия ClickHouse не поддерживает AI**

AI функция доступна только в новых версиях ClickHouse Client (24.x+).

Проверка:
```bash
clickhouse-client --version
# Должно быть 24.x.x или выше
```

#### Как диагностировать:

**Шаг 1: Проверьте что AI настроен**

```bash
cat ~/.config/clickhouse/config.yaml
```

Должны быть эти строки:
```yaml
ai:
  provider: openai  # или anthropic
  api_key: sk-...   # ваш реальный ключ
  base_url: https://openrouter.ai/api/v1  # если используете OpenRouter
```

**Шаг 2: Проверьте что AI работает**

Выполните простой тестовый запрос:
```sql
clickhouse-client
:) ?? сколько баз данных в системе
```

**Если AI работает**, вы увидите:
```
Starting AI SQL generation with schema discovery...
──────────────────────────────────────────────────
🔍 list_databases
   ➜ system, default, ...
✨ SQL query generated successfully!
──────────────────────────────────────────────────

SELECT count() FROM system.databases
```

**Если AI НЕ работает**, вы увидите:
- Ошибку типа "AI features are not configured"
- Или запрос просто не выполнится
- Или выполнится что-то другое

**Шаг 3: Проверьте логи ClickHouse**

Если подключаетесь к удаленному серверу, проверьте что AI функция включена на сервере:
```sql
SELECT * FROM system.settings WHERE name LIKE '%ai%';
```

#### Решения:

**Решение 1: Правильно настройте AI**

Создайте или исправьте `~/.config/clickhouse/config.yaml`:

```yaml
host: ваш-хост
port: 9440
user: ваш-пользователь
password: ваш-пароль
database: ваша-база
secure: true

openSSL:
  client:
    caConfig: /root/.clickhouse-client/root.crt

ai:
  provider: openai
  api_key: ваш-ключ-openrouter
  base_url: https://openrouter.ai/api/v1
  model: anthropic/claude-3.5-sonnet
  temperature: 0.0
  max_tokens: 1000
  timeout_seconds: 30
  enable_schema_access: true
```

**Решение 2: Используйте переменные окружения**

Если не хотите создавать файл конфигурации:
```bash
export OPENAI_API_KEY="ваш-ключ"
# или
export ANTHROPIC_API_KEY="ваш-ключ"

clickhouse-client --host ваш-хост --port 9440 --secure
```

**Решение 3: Проверьте версию ClickHouse**

Обновите ClickHouse Client до последней версии:
```bash
# Linux
sudo apt-get update
sudo apt-get install --only-upgrade clickhouse-client

# macOS
brew upgrade clickhouse
```

**Решение 4: Используйте Python программу вместо CLI**

Если `??` в CLI не работает, используйте Python программу из репозитория:
```bash
cd /path/to/text_to_clickhouse_sql
python3 text_to_sql.py
```

В Python программе AI точно будет работать, и вы сможете контролировать выполнение запросов.

#### Как понять что проблема именно в отсутствии AI:

✅ **AI работает, если:**
- Видите "Starting AI SQL generation..."
- Видите эмодзи 🔍 и процесс исследования схемы
- Токены тратятся в OpenRouter/OpenAI/Anthropic

❌ **AI НЕ работает, если:**
- Не видите "Starting AI SQL generation..."
- Запрос выполняется мгновенно (без задержки)
- Токены не тратятся
- Получаете неожиданные результаты

#### Пример отладки:

```bash
# 1. Проверьте конфиг
cat ~/.config/clickhouse/config.yaml

# 2. Проверьте версию
clickhouse-client --version

# 3. Попробуйте простой AI запрос
clickhouse-client
:) ?? какая версия сервера

# 4. Если не работает, проверьте переменные окружения
echo $OPENAI_API_KEY
echo $ANTHROPIC_API_KEY

# 5. Если всё настроено, но не работает - используйте Python программу
python3 text_to_sql.py
```

## Советы по использованию

### 1. Изучите вашу схему базы данных

Перед использованием AI, полезно знать структуру:

```sql
-- Список таблиц
SHOW TABLES;

-- Структура конкретной таблицы
DESCRIBE TABLE your_table;

-- Или через AI:
:) ?? какие таблицы есть в базе
:) ?? покажи структуру таблицы visits
```

### 2. Сохраняйте удачные запросы

Когда AI создаст хороший запрос, сохраните его:

```bash
# Создайте файл с вашими запросами
nano ~/my_queries.sql

# Пример содержимого:
# -- Популярные страницы
# SELECT page_url, COUNT(*) as visits
# FROM visits_complete
# GROUP BY page_url
# ORDER BY visits DESC
# LIMIT 10;
```

### 3. Комбинируйте AI и обычный SQL

Используйте AI для быстрого прототипирования, потом доработайте запрос вручную:

```sql
-- 1. Сначала используйте AI для основы
:) ?? покажи визиты по дням

-- 2. Скопируйте сгенерированный SQL
-- 3. Доработайте его под ваши нужды
SELECT
    toDate(visit_date) as date,
    COUNT(*) as visits,
    COUNT(DISTINCT user_id) as unique_users,
    AVG(duration) as avg_duration
FROM visits_complete
WHERE visit_date >= today() - INTERVAL 30 DAY
GROUP BY date
ORDER BY date DESC;
```

### 4. Используйте алиасы для часто используемых команд

Добавьте в `~/.bashrc` или `~/.zshrc`:

```bash
# Быстрое подключение к ClickHouse
alias ch='clickhouse-client --host myhost.net --port 9440 --secure'

# Подключение с AI
alias chai='clickhouse-client --host myhost.net --port 9440 --secure --config-file ~/.config/clickhouse/config.yaml'
```

Теперь можно просто писать:
```bash
chai
```

### 5. Настройка истории команд

ClickHouse CLI сохраняет историю в `~/.clickhouse-client-history`. Используйте стрелки ↑↓ для навигации.

Полезные настройки:
```yaml
# ~/.config/clickhouse/config.yaml
history_file: ~/.clickhouse-client-history
max_client_connections: 1000
```

## Сравнение с Python программой

| Аспект | ClickHouse CLI напрямую | Python программа (text_to_sql.py) |
|--------|------------------------|-----------------------------------|
| **Установка** | Только clickhouse-client | Python + зависимости + clickhouse-client |
| **Скорость запуска** | Мгновенно | ~1-2 секунды |
| **Интерфейс** | Командная строка | Интерактивная оболочка |
| **Форматирование** | Стандартный вывод ClickHouse | Красивые таблицы |
| **Гибкость** | Полный доступ ко всем функциям | Ограничено программой |
| **Удобство** | Для опытных пользователей | Для начинающих |
| **Обычный SQL** | ✅ Да | ✅ Да |
| **AI запросы** | ✅ Да (с ??) | ✅ Да (автоматически) |
| **Схема БД** | DESCRIBE TABLE | Команда 'schema' |

## Заключение

Теперь вы знаете как использовать ClickHouse CLI напрямую с AI функцией! Основные шаги:

1. ✅ Установить `clickhouse-client`
2. ✅ Настроить AI (переменная окружения или config файл)
3. ✅ Подключиться к ClickHouse (используйте порт 9440)
4. ✅ Использовать `??` для AI запросов

**Быстрый старт:**

```bash
# 1. Установите переменную окружения
export OPENAI_API_KEY="ваш-ключ"

# 2. Подключитесь к ClickHouse
clickhouse-client \
  --host ваш-хост \
  --port 9440 \
  --user ваш-пользователь \
  --password ваш-пароль \
  --database ваша-база \
  --secure

# 3. Используйте AI!
:) ?? покажи последние 10 записей
```

**Полезные ссылки:**

- [Официальная документация ClickHouse CLI](https://clickhouse.com/docs/en/interfaces/cli)
- [AI SQL Generation в ClickHouse](https://clickhouse.com/docs/en/interfaces/cli#ai-sql-generation)
- [OpenRouter](https://openrouter.ai/)
- [OpenAI](https://platform.openai.com/)
- [Anthropic](https://console.anthropic.com/)

Удачи в работе с ClickHouse! 🚀
